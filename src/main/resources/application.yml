spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/notificationdb
    username: notification
    password: notification

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true